Begin program:
	Set x equal to 76.
	Set y equal to 343.
	If x is not equal to y:
		Output "Not equal".
		If x is less than y:
			Output "x less than y".
			If y is greater than x:
				Output "y greater than x".
			End if.
		End if.
	End if.
End program.